"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.checkFunctionAttributes = void 0;
const errors_1 = require("../errors");
function checkFunctionAttributes(isAbstract, attrs, ref) {
    const k = new Set();
    for (const a of attrs) {
        if (k.has(a.type)) {
            (0, errors_1.throwCompilationError)(`Duplicate function attribute "${a.type}"`, a.ref);
        }
        k.add(a.type);
    }
    if (isAbstract) {
        if (!k.has("abstract")) {
            (0, errors_1.throwCompilationError)(`Abstract function doesn't have abstract modifier`, ref);
        }
    }
    else {
        if (k.has("abstract")) {
            (0, errors_1.throwCompilationError)(`Non abstract function have abstract modifier`, ref);
        }
    }
}
exports.checkFunctionAttributes = checkFunctionAttributes;
