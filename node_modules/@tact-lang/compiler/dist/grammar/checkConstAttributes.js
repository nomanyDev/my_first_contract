"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.checkConstAttributes = void 0;
const errors_1 = require("../errors");
function checkConstAttributes(isAbstract, attributes, ref) {
    const k = new Set();
    for (const a of attributes) {
        if (k.has(a.type)) {
            (0, errors_1.throwCompilationError)(`Duplicate function attribute "${a.type}"`, a.ref);
        }
        k.add(a.type);
    }
    if (isAbstract) {
        if (!k.has("abstract")) {
            (0, errors_1.throwCompilationError)(`Abstract function doesn't have abstract modifier`, ref);
        }
    }
    else {
        if (k.has("abstract")) {
            (0, errors_1.throwCompilationError)(`Non abstract function have abstract modifier`, ref);
        }
    }
}
exports.checkConstAttributes = checkConstAttributes;
